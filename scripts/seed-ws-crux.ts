#!/usr/bin/env bun
/**
 * Intentionally empty for public release.
 *
 * This script used to seed the `crux` workstream with a starter corpus
 * (the dogfood thinking that produced Crux itself). The seed was removed
 * before going public to keep specific business context private.
 *
 * If you want a starter corpus on a fresh checkout, populate this file
 * with your own seed: insert a Workstream, some Observations, a Problem,
 * link them via Evidence, etc. See `packages/core/src/db/schema.ts` for
 * the entity shape and `packages/core/src/transitions/` for the
 * structural invariants.
 *
 * `bun run seed` against this file is a no-op.
 */
