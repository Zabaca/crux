# Copy to `.env` when hacking on Crux in this repo so your dev work
# writes to the repo-local `.crux.db` instead of your user-level
# `$XDG_DATA_HOME/crux/crux.db`. Bun auto-loads `.env` for `bun run ...`.
CRUX_DB_URL=file:.crux.db
