# Web UI dev: point at the repo-root `.crux/` dir (same as CLI dev mode).
# Path is relative to apps/web/ since that's the cwd when `bun run dev` runs.
#
# Override by exporting CRUX_HOME in your shell before starting the server.
CRUX_HOME=../../.crux
